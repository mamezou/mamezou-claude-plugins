---
name: harness-review
description: 検知ログを集計し、誤検知と実違反を切り分けて、harness.json の辞書・閾値と rules への反映案を提示する週次レビュー。「検知ログを見て」「/harness-ja:harness-review」で呼び出す。
argument-hint: "[--days N]"
---

# 検知ログの週次レビュー

hook が差し戻した記録を、規則と辞書の改善へ戻す。集計はスクリプトで決定的に行い、
判断(誤検知か実違反か、何を直すか)は本文を読んで行う。反映は承認後に行う。

## 手順

1. 集計を実行し、出力を全文読む。

```bash
bash "${CLAUDE_PLUGIN_ROOT}/skills/harness-review/summarize.sh" $ARGUMENTS
```

2. 種別ごとに、直近の検出例を読んで「誤検知」「実違反」「規則の曖昧さ」に分ける。
   判断がつかない例は、検知ログの該当行の前後(同じ時刻帯の作業ログ)を読んでから決める。
   読まずに「誤検知と思われる」と書かない。
3. 分類ごとに対応案を作る。
   - 誤検知: `.claude/harness.json` の辞書から語を外す、閾値を上げる、除外条件を足す、
     `responseQuality.patterns` からパターン番号を外す。hook 本体の正規表現を変える
     必要があるときは、対象の hook 名と変更案を書く
   - 実違反で規則が既にある: 規則の文言を具体化する案(該当する rules か Output Style の節を引用)
   - 実違反で規則が無い: 規則へ足す1〜2行の追記文(該当する rules か Output Style の節を指定)
4. 常に読み込まれる指示の行数を前回のレビューと比べ、増えていれば削る候補を1つ挙げる。
5. 次の型で報告し、反映の可否を仰ぐ。反映前に編集しない。

```
【報告】
- 期間と件数、前期間比
- 種別上位3つと、それぞれの分類(誤検知 / 実違反 / 規則の曖昧さ)

【確認点1】辞書・閾値の変更
<harness.json の変更前後を JSON の断片で示す>。可否をお願いいたします。

【確認点2】規則の追記
<ファイル名と追記文>。可否をお願いいたします。
```

6. 承認された項目だけ反映する。hook や辞書を変えたら、変えた理由を hook 冒頭のコメントと
   コミットメッセージに残す。

## 判断の基準

- 同じ検出語が期間内に3回以上出ていて、いずれも文脈上まっとうな用法なら誤検知とみなし辞書から外す
- 同じ種別が2週続けて上位3位に入るなら、規則の文言か Output Style の例示が足りていない。
  禁止語を増やす前に、書き方の型(例文)を Output Style へ足す案を先に出す
- 差し戻し上限に達して通過した記録が期間内に5件以上あるなら、その種別の差し戻しは体裁の反復に
  なっている。`responseQuality.patterns` からその番号を外す案を出す
- 変更は1回のレビューで3件まで。効果を次回の件数で確かめてから次を変える

## 完了条件

- 集計出力を読み、種別上位3つに分類と対応案が付いている
- 反映は承認された項目のみで、変更の理由が hook のコメントとコミットメッセージに残っている
